# 产品挑选可视化网页 — TODO 清单

目标：在本工程实现网页界面，让用户清晰查看已选产品、横向对比、统计总价，并通过链接跳转购物页。产品清单**不与飞书或其他文档同步**。

**新增 / 删除产品**：仅通过**网页界面**完成；**不提供**供终端用户调用的 HTTP 接口来「远程新增」产品（避免与日常用法混淆）。若日后需要脚本批量灌入，可作为单独可选能力再评估。

（列字段可参考 [飞书 Wiki 清单思路](https://my.feishu.cn/wiki/KnMuwfFioi6BH3kvHhWc7YDGnSJ?from=from_copylink)，仅作人工对照。）

---

## 1. 需求与范围

- [ ] **1.1** 冻结产品模型字段（首版）：`id`、`name`、`unitPrice`（价格）、`purchaseUrl`（产品链接）；可选扩展 `quantity`、`spec`、`note` 等。
- [ ] **1.2** **添加产品**：列表页提供按钮；点击后进入**独立路由页面**（如 `/add`），用户填写产品名、价格、链接，提交后将新产品**追加**进列表并返回列表页。
- [ ] **1.3** **删除产品**：列表每一行提供删除按钮，删除对应 `id` 的项并更新展示与持久化。
- [ ] **1.4** 数据持久化：使用浏览器 **localStorage**（或等价）保存列表，刷新不丢失。

---

## 2. 核心界面 — 列表页

- [ ] **2.1** 表格或卡片：名称、单价、小计（首版可按数量 1）、购买链接（新窗口打开）、**删除**操作。
- [ ] **2.2** **总价**：列表下方展示当前所有行金额合计。
- [ ] **2.3** 购买链接：`target="_blank"` + `rel="noopener noreferrer"`。
- [ ] **2.4** 显著入口：**「添加产品」**按钮 → 导航至添加页（非弹窗内嵌复杂表单也可，以「新页面」为准）。

---

## 3. 添加产品页（独立页面）

- [ ] **3.1** 表单字段：产品名、价格（数字校验）、产品链接（URL 或非空校验）。
- [ ] **3.2** 提交：生成唯一 `id`，写入全局清单状态并持久化，`navigate` 回列表页；失败时展示错误提示。
- [ ] **3.3** 取消或返回：链回列表页且不提交。

---

## 4. 工程脚手架

- [ ] **4.1** 前端：Vite + React + TypeScript + **React Router**（多页路由）。
- [ ] **4.2** 全局状态：Context 或轻量 store，供列表页与添加页读写同一 `products` 数组。
- [ ] **4.3**（可选）ESLint / Prettier、响应式布局。

---

## 5. 可选增强（非首版必做）

- [ ] **5.1** 行内编辑单价 / 数量、按分类筛选。
- [ ] **5.2** 导出 CSV、打印友好样式。
- [ ] **5.3** 暗色模式。

---

## 6. 质量与交付

- [ ] **6.1** `README.md`：`npm install` / `npm run dev` / `npm run build`；需明确支持 **Windows**（PowerShell/cmd）、macOS、Linux，无 Bash 专有脚本。
- [ ] **6.2** 自测：空列表、连续添加、删除首尾行、非法 URL/非数字价格。

---

## 建议实现顺序

1. **§4** 脚手架与路由 → 2. **§1** 模型与 localStorage → 3. **§2** 列表 + 删除 + 总价 → 4. **§3** 添加页 → 5. **§6**。

---

*文档随开发进度勾选 `[ ]` → `[x]`，并可在各节下补充子任务与负责人。*
